export {LineSelectionProvider, useLineSelection} from './LineSelectionContext';
export type {LineSelectionContextType} from './LineSelectionContext';
// Note: VerseSelection is now handled by Redux (verseSelectionSlice)
// VerseSelectionContext is deprecated but kept for backwards compatibility if needed
